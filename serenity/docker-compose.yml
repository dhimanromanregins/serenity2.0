version: '3.8'

services:
  django:
    build: .
    working_dir: /serenity
    volumes:
      - .:/serenity
    ports:
      - "8000:8000"
    command: python manage.py runserver 0.0.0.0:8000
    environment:
      - PYTHONUNBUFFERED=1
      - DJANGO_SETTINGS_MODULE=serenity.settings
